name: Build Windows

on:
  workflow_dispatch:
  push:
    branches: ['build/windows-installer']

permissions:
  contents: write

jobs:
  make:
    name: Make Windows x64
    runs-on: windows-2022
    timeout-minutes: 45
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24.18.0
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Test the native core against Node.js
        run: npm run test:native

      # Every build shipped as 0.1.0, so Squirrel could not tell two builds
      # apart and installed over itself. Give each build its own version.
      - name: Version this build
        shell: bash
        run: npm version "0.1.${GITHUB_RUN_NUMBER}" --no-git-tag-version --allow-same-version

      - name: Make Windows distributables
        run: npm run make -- --platform=win32 --arch=x64

      - name: Publish distributables to a GitHub Release
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          tag="windows-build"
          mapfile -t files < <(find out/make -type f \( -name '*.exe' -o -name '*.zip' \))
          if [ "${#files[@]}" -eq 0 ]; then
            echo "No distributables found under out/make" >&2
            exit 1
          fi
          printf 'Publishing: %s\n' "${files[@]}"
          gh release delete "$tag" --yes --cleanup-tag 2>/dev/null || true
          gh release create "$tag" "${files[@]}" \
            --title "Flyoff — Windows x64" \
            --notes "Automated Windows x64 build: Squirrel installer (Setup.exe) and portable zip. Commit ${GITHUB_SHA}." \
            --prerelease
